# Regions to include (comma-separated ISO codes or custom keys)
GHS_REGIONS=US,EU,JP

# Local paths
GHS_LOG_DIR=./logs
GHS_ATT_DIR=./attestations

# Optional: auto-post targets
GHS_OAA_URL=
GHS_LEDGER_URL=
GHS_BEARER=

# Validation & Shield
GHS_SCHEMA_PATH=./schema/pulse.schema.json
GHS_ENFORCE_SCHEMA=1
GHS_ENFORCE_SHIELD=1
